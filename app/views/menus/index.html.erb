<h1><%= t(:list_menu) %></h1>

<%= link_to t(:create_new_menu), new_establishment_menu_path %>

<div>
    <% @menus.each do |menu|%>
        <%= link_to menu.name, establishment_menu_path(menu) %>
    <% end %>
</div>