<%= link_to t(:create_new_menu), new_establishment_menu_path(current_user.establishment) %>

<div>
    <% @menus.each do |menu|%>
        <%= link_to menu.name, establishment_menu_path(current_user.establishment, menu) %>
    <% end %>
</div>